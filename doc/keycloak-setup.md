# Keycloak Setup

## Roles

Add a role
---

Goto `Realm roles -> Create Role` to create a role.

Make the roles visible in the jwt.
---

Goto `Client Scopes -> roles -> Mappers -> realm roles -> Add to ID Token` and turn it on.
